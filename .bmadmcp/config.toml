# bmad-mcp-server project-local config.
# See .bmadmcp/config.example.toml for full schema.

[clickup]
# pinned_space_id        = ""
# pinned_space_name      = ""
# pinned_backlog_list_id = ""

[clickup_create_epic]

[clickup_create_story]
# allow_no_epic = true   # Set false to always require an epic parent (default: true)

[docs]
# This repo's architecture doc lives at docs/architecture.md, not the default
# planning-artifacts/architecture.md (which does not exist in this repo).
architecture_path = "docs/architecture.md"
